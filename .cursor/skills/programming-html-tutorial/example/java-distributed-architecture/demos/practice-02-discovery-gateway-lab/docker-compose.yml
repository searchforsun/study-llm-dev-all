# ShopFlow 本地 Eureka（可选 Docker 方式）
# 推荐：直接用 Maven 启动 discovery-server（见 README）
services:
  discovery-server:
    build:
      context: .
      dockerfile: Dockerfile.discovery
    ports:
      - "8761:8761"
    environment:
      SPRING_PROFILES_ACTIVE: docker
