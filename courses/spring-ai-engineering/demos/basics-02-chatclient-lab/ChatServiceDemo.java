// 课程示意 — 需 Spring Boot 工程运行
// return chatClient.prompt().user(q).call().content();
public class ChatServiceDemo {
  public static String demoReply() {
    return "[demo] ChatClient 将在此返回模型文本";
  }
}
