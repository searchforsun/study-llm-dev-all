# Demo：RAG Pipeline 实现

对应章节：[RAG Pipeline 实现](../../index.html#ch-practice-02-rag-pipeline)

根据 `rag-flow.md` 绘制 Mermaid 序列图，标注 QuestionAnswerAdvisor 注入 context 的步骤。

## 步骤

1. 阅读 `rag-flow.md` 五步流水线。
2. 在笔记绘制 sequenceDiagram（上传 → Reader → Splitter → VectorStore → 提问 → Advisor → LLM → citations）。
3. 标注 Advisor 注入点与「无上下文不编造」system 约束。

## 验收

- [ ] 序列图含 5 步且标注 Advisor 注入点
- [ ] 完成章节测验
