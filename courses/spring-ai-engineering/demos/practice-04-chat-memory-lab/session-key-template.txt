# CorpAssist Chat Memory Redis 键模板

生产键：corpassist:chat:{tenantId}:{userId}:{conversationId}
TTL：24h（可配置 corpassist.memory.ttl-hours）

eval 影子前缀：corpassist:chat:eval:{runId}:{conversationId}
（压测/评测不得写入生产 tenant 键空间）

多 Tab：同一 conversationId 共享列表；移动端新开会话应生成新 conversationId。
