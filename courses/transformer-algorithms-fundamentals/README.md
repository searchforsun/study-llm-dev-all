# 大模型核心算法：Transformer 与架构演进

```bash
# 仓库根目录
node scripts/serve-courses.mjs
# 打开 http://localhost:3000/transformer-algorithms-fundamentals/
```

进度文件：`progress.local.json`（bootstrap 时创建，学习时自动更新）

源文件：`chapters/*.html` → assemble → `index.html`
