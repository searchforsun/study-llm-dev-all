controls:
  SEC-01: { name: input_moderation, blocking: true }
  SEC-02: { name: output_moderation, blocking: true }
  SEC-03: { name: injection_guard, blocking: true }
  SEC-04: { name: pii_redaction, blocking: true }
  SEC-05: { name: tool_authz, blocking: true }
  SEC-06: { name: refusal_policy, blocking: true }
  SEC-07: { name: audit_emit, blocking: true }
  SEC-08: { name: red_team_smoke, blocking: true }
