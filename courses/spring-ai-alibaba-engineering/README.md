# Spring AI Alibaba 工程体系

13 章 · 3 阶段：DashScope/扩展 → 治理运维 → LangGraph 对照与 S3 交付。可运行 Demo 见 `demos/corpassist-saa-workbench/`。

## 启动

在 `courses/` 根目录：

```bash
npx --yes serve .
```

打开 [http://localhost:3000/spring-ai-alibaba-engineering/](http://localhost:3000/spring-ai-alibaba-engineering/)

## 维护

- 改章节：`chapters/*.html`；术语/测验写入 `course.json`（勿用 `manifest-*.json`）→ assemble 生成 `index.html`
- 勿手改 `index.html` 正文
