# Demo：与 LangGraph 对照总结

对应章节：[与 LangGraph 对照总结](../../index.html#ch-advanced-05-compare-langgraph)

## 目标
完成 LangGraph ↔ SAA 概念映射表。

## 操作步骤
1. 用 Python 写一个简单的 StateGraph（validate → manager 两个节点）
2. 用 Java SAA 重写同一个 Graph
3. 在 mapping-table.md 中填写全部映射关系
4. 对比两边的代码风格和类型系统

## 验收
完成 mapping-table.md 中全部 10+ 行映射。

## 练习
补上 interrupt ↔ InterruptNode 在 mapping-table 中的对照说明。
