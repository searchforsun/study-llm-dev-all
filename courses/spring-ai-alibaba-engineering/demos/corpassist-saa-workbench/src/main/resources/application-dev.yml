# 多环境示例：mvn spring-boot:run -Dspring-boot.run.profiles=dev
spring:
  ai:
    dashscope:
      chat:
        options:
          model: qwen-turbo
          temperature: 0.5

logging:
  level:
    com.corpassist: DEBUG
