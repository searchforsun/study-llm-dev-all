# advanced-02-domestic-stack-lab

信创选型与混合路由实验。

| 文件 | 用途 |
|------|------|
| `vendor-matrix.md` | 通义 / 本地 / 昇腾 对比表（填空） |
| `llm-routing.domestic.example.yaml` | 按 dataClass 路由示例 |

## 验收

- [ ] 完成 `vendor-matrix.md` 三行对比
- [ ] `llm-routing.domestic.example.yaml` 中 L4 路由到 `private-qwen` 且无公网出口
- [ ] 采购打分表至少 6 项已标注证据来源

**验收命令（文档型）**：对照 `procurement-scorecard.md` 逐项勾选。
