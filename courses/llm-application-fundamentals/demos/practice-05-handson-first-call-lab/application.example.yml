# 合并进 Spring Boot application.yml — 指向 CorpAssist LLM 网关（OpenAI 兼容）
spring:
  ai:
    openai:
      api-key: ${CORPASSIST_LLM_KEY}
      base-url: ${CORPASSIST_LLM_BASE_URL:https://llm-gateway.corpassist.internal/v1}
      chat:
        options:
          model: ${CORPASSIST_LLM_MODEL:corpassist-fast}
          temperature: 0.2
          max-tokens: 512

# 冒烟接口实现见章节 practice-05-handson-first-call 中 ChatClient + Controller 示例
