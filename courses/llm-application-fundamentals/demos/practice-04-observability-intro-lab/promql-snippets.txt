# CorpAssist — PromQL 思路（示意，指标名以实际为准）

# P95 端到端延迟（5 分钟窗口）
histogram_quantile(
  0.95,
  sum(rate(corpassist_chat_e2e_seconds_bucket{scene="rag_qa"}[5m])) by (le)
)

# 单租户 Token 小时环比（需 recording rule 或 compare 昨日同期）
sum(increase(corpassist_llm_tokens_total{tenant="hr"}[1h]))
  /
sum(increase(corpassist_llm_tokens_total{tenant="hr"}[1h] offset 1d))

# 评测通过率低于 SLO
corpassist_eval_pass_rate{suite="policy-qa-golden-v3"} < 0.85
