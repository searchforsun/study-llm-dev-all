# advanced-01-multimodal-intro-lab

CorpAssist 附件问答与多模态入库实验。

| 文件 | 用途 |
|------|------|
| `attachment-pipeline.md` | 扫描 PDF → OCR → 分块 → Embedding 检查清单 |
| `sample-multimodal-request.json` | 图文消息 API 请求示例（勿含真实密钥） |

## 验收

- [ ] 能画出「入库路径」与「在线问答路径」两条链路
- [ ] 能说明 OCR 发生在入库而非在线问答主路径
- [ ] 完成 `ocr-vs-vision-routing.md` 中的文档类型决策表

**验收命令（文档型）**：打开 `attachment-pipeline.md`，按检查清单逐项勾选。
