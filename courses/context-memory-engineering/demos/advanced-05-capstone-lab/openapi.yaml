openapi: 3.0.3
info:
  title: CorpAssist Memory API
  version: 1.0.0
paths:
  /v1/session/{tenant}/{user_id}/messages:
    get:
      summary: 获取会话消息
    post:
      summary: 追加会话消息
  /v1/memory/{user_id}/recall:
    post:
      summary: 召回长期记忆
  /v1/memory/{user_id}/facts:
    get:
      summary: 列出用户事实
  /v1/memory/{user_id}:
    delete:
      summary: GDPR 删除用户记忆
