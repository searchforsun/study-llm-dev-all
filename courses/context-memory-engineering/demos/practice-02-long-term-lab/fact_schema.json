{
  "schema": "corpassist-fact-v1",
  "required": ["tenant_id", "user_id", "fact_text"],
  "optional": ["fact_type", "expires_at", "source_turn"],
  "fact_types": ["preference", "identity", "order", "constraint"]
}
